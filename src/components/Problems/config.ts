// Default to a local JSON file served from /public so the app works out-of-the-box
// You can later replace this with a raw GitHub URL
export const GITHUB_PROBLEMS_RAW_URL: string | undefined = "/problems.json";


